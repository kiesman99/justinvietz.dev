---
import HeaderLink from "./HeaderLink.astro";
import { SITE_TITLE } from "../consts";
// import ThemeSwitcher from "./ThemeSwitcher";
import ThemeSwitcher from "./ThemeSwitcher.svelte";
---

<div class=" top-0 left-0 right-0 fixed bg-white h-14 flex items-center justify-center shadow-md">
    <p class="text-xl font-bold">This page is under construction</p>
</div>

<header
  class="flex flex-col container mx-auto px-5 py-2 items-center gap-5 mt-20"
>
  <h2 class="text-3xl dark:text-white">
    {SITE_TITLE}
  </h2>
  <nav
    class="flex flex-row gap-3"
  >
    <HeaderLink href={`/`}>Home</HeaderLink>
    <HeaderLink href={`/blog`}>Blog</HeaderLink>
  </nav>
  <ThemeSwitcher client:load/>
</header>